kind = "web"
previewPath = "/"
title = "Travel Survey"
version = "1.0.0"
id = "artifacts/travel-survey"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25048

[services.development]
run = "pnpm --filter @workspace/travel-survey run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/travel-survey", "run", "build" ]
publicDir = "artifacts/travel-survey/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25048"
BASE_PATH = "/"
